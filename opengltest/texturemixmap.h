#ifndef TEXTUREMIXMAP_H
#define TEXTUREMIXMAP_H

#include <QGLWidget>

class texturemixmap : public QGLWidget
{
    Q_OBJECT
public:
    explicit texturemixmap(QObject *parent = 0);

signals:

public slots:

};

#endif // TEXTUREMIXMAP_H
